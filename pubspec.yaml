name: mood_journal
description: AI-powered mood tracking app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # UI
  cupertino_icons: ^1.0.2
  flutter_svg: ^2.0.9
  flutter_native_splash: ^2.3.10  # ← AJOUTEZ CETTE LIGNE

  
  # State Management
  provider: ^6.1.1
  
  # Storage Local
  shared_preferences: ^2.2.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  
  # Dates
  intl: ^0.19.0
  
  # Notifications
  #flutter_local_notifications: ^16.3.0
  
  # Icons supplémentaires
  lucide_icons: 0.257.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1
  hive_generator: ^2.0.1
  build_runner: ^2.4.7

flutter:
  uses-material-design: true
  
